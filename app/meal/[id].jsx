import { Text, View } from "react-native";

const MealDetail = () => {
  // This component will display the details of a meal
  // You can fetch the meal details using the meal ID from the route params
  // and display them here.a
  // For now, we will just return a placeholder view
  // You can use the useRouter hook to get the meal ID from the URL
  return (
    <View>
      <Text>Meal Detail</Text>
    </View>
  );
};

export default MealDetail;
